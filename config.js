'use strict';

/**
 *  config file
 */
let config = {
	// mongodb 配置
  	mongodb: {
  		db: 'mongodb://127.0.0.1/nc-example',
  		poolSize: 5
  	},

  	// mysql 配置
  	mysql: ''
};

module.exports = config;